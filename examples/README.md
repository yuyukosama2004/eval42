# Examples

- `mock-shopping` and `mock-research` are fully offline executable examples.
- `phonemall` and `groundedseek` are mapping templates for the two validated consumers. Copy a
  reviewed project dataset beside the config before running them.

The target templates intentionally contain no production URL, token, customer data, or copied
target-project implementation.
